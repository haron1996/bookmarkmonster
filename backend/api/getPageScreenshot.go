package api

/*
screenshotQuality := 100

	byte, err := page.Screenshot(true, &proto.PageCaptureScreenshot{
		Format:  proto.PageCaptureScreenshotFormat("png"),
		Quality: &screenshotQuality,
		//Clip:                  &proto.PageViewport{},
		FromSurface:           true,
		CaptureBeyondViewport: false,
		OptimizeForSpeed:      true,
	})
	if err != nil {
		panic(err)
	}

	f, err := os.Create("screenshot.png")
	if err != nil {
		panic(err)
	}

	defer f.Close()

	_, err = f.Write(byte)
	if err != nil {
		panic(err)
	}
*/
